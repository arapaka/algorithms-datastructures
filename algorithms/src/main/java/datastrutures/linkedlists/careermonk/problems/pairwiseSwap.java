package datastrutures.linkedlists.careermonk.problems;

/**
 * Created by archithrapaka on 2/19/17.
 */
public class pairwiseSwap {

//    public ListNode pairWiseSwap(ListNode head) {
//         if(head == null || head.next == null){
//             return head;
//         }
//         ListNode prev = head;
//         ListNode curr = head.next;
//         head = curr;
//
//         while (curr!= null || prev!= null) {
//             ListNode next = curr.next;
//
//             // if next is null (on last two nodes ) or next.next is null (three nodes )
//             if(next == null || next.next == null) {
//                 prev.next = next;
//                 return head;
//              }
//
//              // change the pointers for prev to next.next
//              prev.next = next.next;
//
//             // Update previous and curr;
//
//         }
//    }



    public static void main(String[] args){

    }
}
